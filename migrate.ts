import * as fs from 'fs';
import * as path from 'path';

async function migrate() {
  // It's a TS file with an export, we can import it in Node dynamically if we strip types or use a loader
}
migrate();
